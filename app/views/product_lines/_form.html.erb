<%= form_with(model: product_line) do |form| %>
  <% if product_line.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_line.errors.count, "error") %> prohibited this product_line from being saved:</h2>

      <ul>
        <% product_line.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label "#{:company}:" %>
    <%= form.text_field :company %>
  </div>

  <div>
    <%= form.label "#{:logo}:" %>
    <%= form.text_field :logo %>
  </div>

  <div>
    <%= form.label "#{:description}:" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label "#{:website}:" %>
    <%= form.text_field :website %>
  </div>

  <div>
    <%= form.label "#{:image}:" %>
    <%= form.file_field :image %>
  </div>

  <hr width="15%">

  <div>
    <%= form.submit %>
  </div>
<% end %>
